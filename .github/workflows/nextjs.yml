/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/turbine-heat-power',
  images: {
    unoptimized: true,
  },
  trailingSlash: true,
  assetPrefix: '/turbine-heat-power/',
}

module.exports = nextConfig
